import "reflect-metadata"

describe('commands', () => {

    describe('matches', () => {
        it('lists last 20 matches');

        it('shows relative date (Today 15:30 for example)');

        it('puts latest match is on top');

        it('shows a helpful message when you have no matches entered yet');

        it('shows the winner at the left side, even when the opponent has entered it')

        it('shows the winner at the left side');

        it('shows reverses the score when the opponent entered a loss');
    });
})
